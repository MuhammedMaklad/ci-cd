name: First Workflow
on:
  push:
    branches:
      - main
  # pull_request:
  #   branches:
  #     - main
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Welcome Message
        run: echo "Welcome to the First Workflow!"
